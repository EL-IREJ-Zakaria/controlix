Place optional `.ps1` helper scripts here if you want to version them alongside the agent.

The current mobile workflow stores task scripts directly in `agent/data/tasks.json`, so this folder is optional.
